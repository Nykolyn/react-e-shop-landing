import styled from 'styled-components';
import {
  space,
  layout,
  color,
  flexbox,
  border,
  position,
  boxShadow,
  typography,
  shadow,
} from 'styled-system';

const Box = styled.div`
  ${space}
  ${layout}
  ${color}
  ${flexbox}
  ${border}
  ${position}
  ${boxShadow}
  ${typography}
  ${shadow}
`;

export default Box;
